    // res.writeHead(200, {'Content-Type': 'text/html'});


    // writeHead is a method that allows you to set the status code and headers of the response in one step. It takes two arguments: the status code (e.g., 200 for success) and an object containing the headers (e.g., {'Content-Type': 'text/html'}). Using writeHead is a convenient way to set both the status code and headers at the same time, rather than setting them separately using res.statusCode and res.setHeader. In this example, we are setting the status code to 200 and the Content-Type header to 'text/html', which indicates that the response will contain HTML content. After setting the headers, we can use res.end() to send the response back to the client.
    
    // difference between res.writeHead and res.setHeader is that res.writeHead sets both the status code and headers in one step, while res.setHeader is used to set individual headers separately. res.writeHead is a convenient method for setting the status code and headers together, while res.setHeader allows you to set headers one at a time. In this example, we are using res.setHeader to set the Content-Type header, and then we can use res.statusCode to set the status code separately before sending the response with res.end().

    // Priority of writeHead and setHeader is that if you use res.writeHead after using res.setHeader, the headers set with res.setHeader will be overridden by the headers specified in res.writeHead. This is because res.writeHead sets the status code and headers in one step, and it will overwrite any headers that were previously set with res.setHeader. Therefore, if you want to use both methods, you should call res.setHeader before calling res.writeHead to ensure that the headers you set with res.setHeader are included in the final response. In this example, we are using res.setHeader to set the Content-Type header, and then we can call res.writeHead to set the status code and any additional headers if needed.

    // writehead send response to the client immediately, while setHeader allows you to set headers without sending the response right away. When you call res.writeHead, it sends the response headers and status code to the client immediately, and you can then use res.write or res.end to send the response body. On the other hand, when you use res.setHeader, it allows you to set headers without sending the response immediately. You can set multiple headers using res.setHeader before calling res.end to send the response. This gives you more flexibility in setting headers and controlling when the response is sent to the client. In this example, we are using res.setHeader to set the Content-Type header, and then we can call res.end to send the response back to the client when we are ready.

    // you can do modifications to the headers using setHeader before calling writeHead, but once you call writeHead, the headers are sent to the client and cannot be modified. If you need to change headers after calling writeHead, you would need to use res.setHeader again to set the new headers, but keep in mind that these changes will not affect the response that has already been sent to the client. Therefore, it's important to set all necessary headers using res.setHeader before calling res.writeHead to ensure that the correct headers are included in the response sent to the client. In this example, we are using res.setHeader to set the Content-Type header before calling res.writeHead to send the response with the appropriate headers.

    // What is os agnastic? os agnostic means that the code or application can run on any operating system without requiring specific modifications or dependencies. It is designed to be compatible with multiple operating systems, allowing it to function seamlessly across different platforms. This is achieved by using cross-platform libraries, frameworks, or tools that abstract away the underlying differences between operating systems. By being os agnostic, developers can create applications that can be easily deployed and used on various operating systems without worrying about compatibility issues or platform-specific requirements. In this example, we are using Node.js, which is an os agnostic runtime environment that allows us to write server-side code that can run on different operating systems without modification.

    // so for solving the problem of os agnostic, we can use cross-platform libraries or frameworks that abstract away the differences between operating systems. For example, in Node.js, we can use the built-in 'os' module to access operating system-specific information and functionality in a way that is compatible across different platforms. Additionally, we can use tools like Docker to containerize our applications, allowing them to run consistently across different environments regardless of the underlying operating system. By leveraging these approaches, we can ensure that our code remains os agnostic and can be executed on various operating systems without requiring specific modifications or dependencies.

    // and also use import.meta to get the current file path and directory, which can help in handling file paths in a way that is compatible across different operating systems. This allows us to write code that can work seamlessly on different platforms without worrying about path separators or other OS-specific issues. By using import.meta, we can ensure that our code remains os agnostic and can be executed on various operating systems without modification.


    // relative path and absolute path are two ways to specify the location of a file or directory in a file system. A relative path is a path that is relative to the current working directory, while an absolute path is a complete path that specifies the location of a file or directory from the root of the file system. For example, if the current working directory is /home/user and you want to access a file called data.txt located in the same directory, you can use a relative path like ./data.txt. On the other hand, if you want to access the same file using an absolute path, you would use /home/user/data.txt. Relative paths are often used when referencing files within a project, while absolute paths are used when referencing files outside of the project or when specifying a specific location in the file system.

    // Path module in Node.js is a built-in module that provides utilities for working with file and directory paths. It allows you to manipulate and work with file paths in a platform-independent way. The path module provides methods for joining, resolving, and normalizing file paths, as well as extracting information such as the directory name, base name, and file extension from a given path. It is commonly used when working with file systems in Node.js applications to ensure that file paths are handled correctly across different operating systems. For example, you can use the path.join() method to join multiple path segments together, ensuring that the correct path separators are used based on the operating system. Overall, the path module is an essential tool for handling file paths in Node.js applications and helps ensure that your code works consistently across different environments.

    // process.cwd() is a method in Node.js that returns the current working directory of the Node.js process. It provides the absolute path to the directory from which the Node.js process was launched. This method is useful when you need to access files or directories relative to the current working directory, as it allows you to construct file paths based on the location of your application. For example, if you have a file called data.txt in the same directory as your Node.js script, you can use process.cwd() to get the current working directory and then join it with the file name to access the file. Overall, process.cwd() is a helpful method for managing file paths and ensuring that your application can access resources correctly regardless of where it is executed from.